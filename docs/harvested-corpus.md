# Harvested ecosystem corpus — status

Fixtures collected from other HOCON implementations' test suites, with
expected outputs regenerated by the reference implementation (Lightbend
typesafe-config). Conventions, layout, and harvest rules:
[`testdata/harvested/README.md`](../testdata/harvested/README.md).

The goal of this corpus is interoperability coverage: configs the wider
ecosystem exercises — especially regression fixtures born from real-world bug
reports — become part of what the o3co family tests against. It is a separate
layer from the spec corpus and does not affect `docs/spec-checklist.md` S-rows
or the compliance-matrix denominator.

## Phase 1 — 2026-07-24

| Source | Pinned commit | Copied files | Roots | `-expected.json` | `.error` | Companions | Excluded upstream files |
|---|---|---:|---:|---:|---:|---:|---:|
| [mikai233/hocon-rs](https://github.com/mikai233/hocon-rs) `resources/` | `574cfb6` | 29 | 26 | 18 | 8 | 3 | 3 |
| [mockersf/hocon.rs](https://github.com/mockersf/hocon.rs) `tests/data/` | `cbaf6d0` | 24 | 22 | 17 | 5 | 0 | 1 |

Exclusions (full reasons in each directory's `PROVENANCE.yaml`): three files
with network-dependent URL includes, one exact content duplicate of an
existing spec-corpus fixture.

## Observations

Recorded for future fixture triage; none of these currently feed the
compliance matrix.

- **Nested `+=`** (`mikai233/add_assign.conf`, `object7.conf`): upstream's own
  expectation resolves the nested self-referential `+=` forms; the reference
  parser rejects them ("Due to current limitations of the config parser, +=
  does not work nested inside a list").
- **Root-level `include` followed by a braced root object**
  (`mockersf/include.conf`, `include_file.conf`): reference parse error — a
  file that uses root braces cannot have content outside them.
- **`include` in value position** (`mockersf/includes_no_separator.conf`):
  parses as unquoted-string concatenation (`b = "include basic.conf"`); no
  inclusion is performed. Upstream performs the inclusion here.
- **`include` nested inside a list element** (`mockersf/test03.conf`,
  `include_and_subst.conf`): reference parser limitation error.
- **Composition-dependent fixtures** (`mockersf/substitution.conf`): resolves
  only when included together with `basic.conf` (as upstream's `include.conf`
  does); standalone resolution fails on `${b}` and is classified as an error
  fixture.
- **Null-key rendering**: upstream mikai233 expectations keep null-valued
  object keys; the reference pipeline renders null == missing at object level,
  matching the spec-corpus convention. Values are otherwise identical for all
  compared success fixtures (numeric formatting differences like `1E10` vs
  `10000000000` are value-equal).

## Planned next phases

1. Wire the four implementations' conformance runners to this corpus
   (per-impl work, tracked separately).
2. Additional file-based sources: `puppetlabs/ruby-hocon` (encoding edge
   cases), `@pushcorn/hocon-parser` (spec-relevant subset of its fixture tree;
   its transform/require/classpath extension fixtures stay out).
3. Inline-string extraction sources: `chimpler/pyhocon`,
   `gurkankaymak/hocon`, Lightbend's inline parser tests.
