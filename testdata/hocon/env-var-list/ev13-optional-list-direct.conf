# ev13: S13c — optional list expansion, isolated from concat context
#
# ev06/ev07 both exercise ${?X[]} but always inside a concat expression
# (e.g. "x = [literal] ${?X[]}"). This fixture tests the simplest possible
# form: a bare optional list substitution as the entire value of a key.
#
# S13C_EV13_MY_LIST_0=a is set, so the expansion yields ["a"].
# Expected: {"x": ["a"]}
#
# Pins the "optional-list with elements present → resolved list" path in
# isolation, separate from concat folding (joinPair) and the no-elements
# optional-removes-key path (ev04).
x = ${?S13C_EV13_MY_LIST[]}
