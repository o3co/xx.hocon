# S18.4: fractional bare-number string with no unit suffix.
# Per-family Lightbend-faithful: bytes uses BigDecimal.toBigInteger (truncate toward zero),
# so fractional is ACCEPTED + TRUNCATED. 1024.5 → 1024 bytes (NOT rounded to 1025).
# Expected: `getBytes("b") == 1024`.
b = "1024.5"
