# S18.4 + accessor invariant: negative bare-number string with no unit suffix.
# Per-family Lightbend-faithful: parser accepts negative as BigInteger(-1),
# but accessor `getBytesBigInteger` REJECTS negative byte sizes (positive-only invariant).
# Diverges from duration/period which allow negative at accessor.
# Expected: `getBytes("b")` raises (accessor-layer rejection, not parse-layer).
b = "-1"
