# S18.4: fractional bare-number string with no unit suffix.
# Per-family Lightbend-faithful: period uses Integer.parseInt (integer-only),
# so fractional is REJECTED. Diverges from duration/bytes which accept fractional.
# Expected: `getPeriod("p")` raises parse/value error.
p = "7.5"
